---@meta BrittisH39-Hades2RogueArchipelago
local public = {}

-- Public surface other plugins could use. Currently only the config is exposed
-- (see main.lua: `public.config = config`).

return public
